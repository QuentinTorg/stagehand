# Author assignment

Implement {{objective}} in {{development_target_path}}, branch {{branch}}, based on {{base_commit}}.
Source issue/request: {{source_reference}}.
Boundaries and relevant context: {{constraints}}.

First explore and discuss a plan with the human in this pane; wait for their approval before editing. Once approved, implement and verify the change, then use `preparing-pull-requests` to publish the feature branch and create its draft PR. No additional approval is needed for that initial draft.

Preserve human-confirmed intent, delivered behavior, verification, limitations, and scope boundaries in the description. Link the source issue using repository conventions; use a closing keyword only if fully resolved.

Follow repository instructions, preserve unrelated work, and keep this assignment within its agreed scope. Do not spawn additional agents. Report the result, PR, verification, and any unresolved decisions normally.

## Delegated-planning modifier

When the human delegated plan approval, replace the human-planning paragraph with the actual approval path: propose the plan to the coordinator, or implement the supplied coordinator-approved plan. Include the bounded human grant and preserve verification and draft-PR preparation. Do not also require human approval; delegation of planning grants no finalization or merge rights.
